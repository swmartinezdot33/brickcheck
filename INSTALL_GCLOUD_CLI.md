# Installing Google Cloud CLI

We need to install gcloud CLI first. Here are the options:

## Option 1: Install via Homebrew (Easiest on Mac)

If you have Homebrew installed:

```bash
brew install --cask google-cloud-sdk
```

After installation, restart your terminal or run:
```bash
source ~/.zshrc
```

## Option 2: Install via Official Installer

1. Download from: https://cloud.google.com/sdk/docs/install
2. Run the installer
3. Follow the prompts

## Option 3: Quick Install Script

Google provides a quick install script:

```bash
curl https://sdk.cloud.google.com | bash
exec -l $SHELL
```

Let me know which method you prefer, or I can help you install it!





